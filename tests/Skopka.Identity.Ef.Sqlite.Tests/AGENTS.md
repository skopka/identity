# SQLite EF Test Instructions

Read `../../AGENTS.md`, `../../src/Skopka.Identity.Ef/AGENTS.md` and
`../../src/Skopka.Identity.Ef.Sqlite/AGENTS.md` first.

Use a real open in-memory SQLite connection with foreign keys enabled. Keep packaged
migration discovery, pending-model checks, profile round-trip, filtered uniqueness,
UTC timestamp ordering and database concurrency covered.
