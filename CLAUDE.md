# Claude Instructions

Основной контекст репозитория находится в [`AGENTS.md`](AGENTS.md).
Перед работой прочитай `AGENTS.md` и используй его как источник архитектурных решений.

Дополнительно для Claude:

- Не начинай с полного погружения во все файлы: сначала используй карту проекта из
  `AGENTS.md`, затем открывай только файлы, связанные с задачей.
- Сохраняй границы между `Abstractions`, `Core`, `Ef` и инфраструктурой.
- Не меняй публичные контракты без явной причины и без учета обратной совместимости.
- После изменений проверь сборку командой `dotnet build Skopka.Identity.slnx`, если это возможно.

